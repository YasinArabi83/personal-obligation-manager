# Copy to .env and fill in real values. Never commit .env itself.

POSTGRES_DB=pom
POSTGRES_USER=pom
POSTGRES_PASSWORD=

MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=

JWT_SIGNING_KEY=
SMS_PROVIDER_API_KEY=

# Domain the app is served on, used by the reverse-proxy (Caddy) for automatic HTTPS
DOMAIN=

ASPNETCORE_ENVIRONMENT=Production
