namespace POM;

/// <summary>
/// Marker file so the Application project is never empty during scaffolding.
/// Use cases and port interfaces arrive in later tasks.
/// </summary>
internal static class ApplicationMarker { }
