namespace POM;

/// <summary>
/// Marker file so the Domain project is never empty during scaffolding.
/// Real domain entities, value objects, and domain events arrive in later tasks.
/// </summary>
internal static class DomainMarker { }
