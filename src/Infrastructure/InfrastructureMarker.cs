namespace POM;

/// <summary>
/// Marker file so the Infrastructure project is never empty during scaffolding.
/// EF Core DbContext and provider adapters arrive in later tasks (0002+).
/// </summary>
internal static class InfrastructureMarker { }
