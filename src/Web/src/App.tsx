/**
 * Personal Obligation Manager — root application component.
 * Placeholder UI during scaffolding (Phase 0). Feature-based folders
 * (/src/features/*) and the shared API client arrive with their tasks.
 */
export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 bg-slate-50 text-slate-900">
      <h1 className="text-2xl font-bold">مدیریت تعهدات شخصی</h1>
      <p className="text-sm text-slate-500">Personal Obligation Manager</p>
    </main>
  );
}
